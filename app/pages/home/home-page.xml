<!--
The markup in NativeScript apps contains a series of user interface components, each
of which NativeScript renders with a platform-specific iOS or Android native control.
You can find a full list of user interface components you can use in your app at
https://docs.nativescript.org/ui/components.
-->
<Page xmlns="http://schemas.nativescript.org/tns.xsd" navigatingTo="navigatingTo" class="page">
 <Label text="{{ accelerometerData }}" />
    <GridLayout horizontalAlignment="center" columns="auto, auto, auto, auto, *" rows="auto, auto, auto, auto, *"> 
        <Button text="Test Dialog" row="0" col="0" tap="{{ onTestDialogButtonTap }}" />
        <Button text="Test Toast" row="0" col="1" tap="{{ onTestToastButtonTap }}" />
        <Button text="Test Notification" row="1" col="0" tap="{{ onTestNotificationButtonTap }}" />
        <Button text="Test Accelerometer" row="1" col="1" tap="{{ onTestAccelerometerButtonTap }}" />
        <Button text="Test GPS" row="2" col="0" tap="{{ onTestGPSButtonTap }}" />
        <Button text="Test Camera" row="2" col="1" tap="{{ onTestCameraButtonTap }}" />
        <Button text="Test Socket" row="3" col="0" tap="{{ onTestSocketButtonTap }}" />
        <Button text="Test Worker" row="3" col="0" tap="{{ onTestWorkerButtonTap }}" />
        <StackLayout row="4", col="0">
          <!--
            <Cam:CameraPlus height="{{ cameraHeight }}" id="camPlus" 
                            loaded="camLoaded" galleryPickerMode="multiple" 
                            enableVideo="true" confirmVideo="false" 
                            saveToGallery="true" showCaptureIcon="true" 
                            showGalleryIcon="true" showToggleIcon="true" 
                            showFlashIcon="true" confirmPhotos="true" 
                            flashOffIcon="icon" autoSquareCrop="true"
                            insetButtons="true" insetButtonsPercent="0.02" 
                            imagesSelectedEvent="{{ imagesSelectedBinding }}" 
                            debug="true">
              
                    <GridLayout rows="auto, *, auto" columns="auto, *, auto">
                        <Button row="0" col="0" text="Flash" class="btn-transparent" tap="{{ toggleFlash }}" />
                        <Button row="0" col="2" text="Camera" class="btn-transparent" tap="{{ toggleCamera }}" />
                        <Image row="1" col="1" horizontalAlignment="center" verticalAlignment="center" src="https://img.clipartfest.com/4ec5e2315cea92482da5546e1e7fca89_-pinterest-cute-clipart-snowman-clipart-transparent-background_300-300.png" stretch="none" />
                        <Button row="2" col="0" text="Gallery" class="btn-transparent" tap="{{ openGallery }}" />
                        <Button row="2" col="2" text="Take" class="btn-transparent" tap="{{ takePic }}" />
                    </GridLayout>
               
            </Cam:CameraPlus>
             -->
        </StackLayout>
    </GridLayout>
</Page>